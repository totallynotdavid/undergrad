name: pages

on:
  workflow_dispatch:
  push:
    paths:
      - "packages/**"
      - "**/*.py"
      - "**/pyproject.toml"
      - "notebooks.toml"
      - "scripts/export_notebooks.py"
      - "scripts/notebooks_manifest.py"
      - "site/**"
      - "mise.toml"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/deploy.yml"

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@ca5ddd015e07666cb1f1340ad6171f5e7afffb3e # v8.2.0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - uses: jdx/mise-action@v3

      - name: Install site dependencies
        run: bun install --cwd site --frozen-lockfile

      - name: Validate site manifest
        run: uv run python scripts/export_notebooks.py validate

      - name: Export notebooks
        run: uv run python scripts/export_notebooks.py export --clean

      - name: Build site
        working-directory: site
        env:
          BASE_PATH: /${{ github.event.repository.name }}
        run: bun run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: site/build

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
        with:
          artifact_name: github-pages
